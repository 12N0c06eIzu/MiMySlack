const state = {
  spaceId: 0,
  spaceList: {},
};

const getters = {};

const mutations = {};

const actions = {};

const spaceFunction = {
  namespaced: true, // 忘れずに
  state,
  getters,
  actions,
  mutations,
};

export default spaceFunction;
